# Copy this file to `.env` and fill in your CNN endpoint.
# Leave it unset to run the in-browser demo heatmap instead.
#
# Vite only exposes variables that start with VITE_ to the app.
VITE_MODEL_API_URL=http://localhost:8000/predict
